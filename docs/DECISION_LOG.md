# Decision Log — Lamer Konekte

| # | Time (MUT) | Decision | Rationale |
|---|---|---|---|
| 1 | 29 14:26 | Development happens in WSL2 Ubuntu (repo lives there), not Windows-native. | That is where the repo and toolchain actually are. PowerShell resource scripts still provided for the Windows host; bash equivalents added for WSL. |
| 2 | 29 14:26 | Python 3.12.13 via `python3.12` for the backend venv. | Brief requires 3.11/3.12; 3.12 is the newest available. |
| 3 | 29 14:27 | Hosted provider built code-complete but gated on `GEMINI_API_KEY`; app defaults to disclosed mock mode until the key exists. | No key is configured. Faking inference is forbidden; blocking all work on the key is worse. |
| 4 | 29 14:27 | Deadline assumed 2026-07-30 13:36 MUT. | No official deadline recorded; repo creation ≈ start of the 24 h window. Documented as an assumption. |
| 5 | 29 14:27 | Kaggle training prepared but not launched. | No Kaggle credentials on the machine. Launch scripts ready for the moment auth exists. |
| 6 | 29 14:27 | Local edge proof kept at P3 and likely skipped. | WSL sees only 7.4 GB RAM; no HF auth/licence. Edge bonus must never be claimed from mock behaviour, and Morisyen bonus is the safer 10-point path. |
| 7 | 29 14:30 | Five P0 species: Octopus cyanea, Lethrinus nebulosus, Siganus sutor, Epinephelus merra, Naso unicornis. | Locally relevant lagoon/reef species with iNaturalist/GBIF presence; octopus required by the brief pending taxonomic verification. |
| 8 | 29 14:30 | SQLModel over raw SQLAlchemy. | Single class serves as Pydantic schema + table; faster under time pressure; brief allows either. |
| 9 | 29 14:30 | fpdf2 for PDF export. | Pure-python, tiny, no system deps. |
| 10 | 29 14:30 | Zustand for frontend state; TanStack Query for server state. | Brief-sanctioned, minimal boilerplate. |
| 11 | 29 14:30 | Fisheries rules dataset marked `verification: pending_official_confirmation`; engine returns `unknown` for any rule lacking a verified source. | We must not invent laws. Rules carry source URL/title/dates; unverified ⇒ `unknown` + "verify against the latest official fisheries notice." |
| 12 | 29 19:40 | Tool registry gap closed by declaring `prepare_catch_declaration`, `submit_mock_declaration`, `queue_for_offline_sync` to the model (12 executable == 12 declared), not by removing them. | All three are non-destructive (a mock declaration draft/submit, an offline-sync queue write) and a fisher's assistant plausibly should be able to trigger them via natural language — e.g. "prepare my declaration for this month." Deleting working, tested code the night before a demo carries more risk than declaring it. `execute()` now rejects any name not in the declared set at runtime; a test pins the two sets equal so the gap cannot reopen silently. |
| 13 | 30 01:50 | Task 1b step 1 (model-list enumeration) run as one live call; no hosted Gemma E2B/E4B exists on this key. | The brief said "discover, don't assume" and the capability matrix had been assuming. The call showed exactly two Gemma models (`gemma-4-26b-a4b-it`, `gemma-4-31b-it`), 256K input each. Consequence: the edge tier was never reachable via the hosted API, so the local negative result (Ollama E2B, false-confident species misidentification) closes that path from a second direction rather than leaving "try E2B hosted" as an untried option. |
| 14 | 30 02:20 | Marine Transport pillar ships on data labelled `synthetic`, not `sample`. | No aisstream key existed at build time, so the committed AIS capture was written by hand against the documented message schema. `sample` in `DataProvenance` means a real capture; using it here would have been a small, invisible lie on every response. The whole pillar — parser, store, brief, endpoint — is built and tested against it, so swapping in live messages changes `data_kind` and nothing else. |
| 15 | 30 02:20 | `/api/demo/reset` clears the `AisPosition` table. | Reset exists to make the demo repeatable, and a transport brief still showing vessels from a previous run has no other way to be cleared. Nothing is lost: with the collector running the rolling window refills from the live feed within seconds, and without it the synthetic seed is rebuilt on the next request. The alternative — treating stored positions as observations too precious to clear — would make the demo non-repeatable to protect data that is by design disposable. |
| 16 | 30 02:20 | Transport merges implemented-but-disabled; `PILLARS_ENABLED` still defaults to `fisheries`. | Task 4a's promise is that landing a pillar module never silently exposes it. A 503 that explains itself is more honest than a 200 serving fixture data nobody asked for. Operators opt in with `PILLARS_ENABLED=fisheries,transport`. |
| 17 | 30 02:55 | **S1-frozen interface change:** `InferenceProvider.chat()` gains an OPTIONAL `system_instruction` parameter, default `None`. `None` preserves today's fisheries behaviour byte-for-byte. Approved by the inference owner (Yadhav); Workstream 3 notified with a veto window. | The frozen `chat()` hardcoded the fisheries `SYSTEM_INSTRUCTION`, which both scopes the model to fisher assistance and orders it to *"always produce valid structured output"*. **Measured, not theorised:** on 30 Jul 2026 a live hosted call asking for a Port Louis arrivals brief returned `{"intent": "other", "reply": "I am an analysis engine for fishers, not a port officer..."}` — a hard refusal wrapped in the catch-assistant envelope. Every non-fisheries pillar hits this: Transport, Tourism, Energy, Blue Finance and Biotech all need scoped prose. The alternatives were worse: duplicating a provider per pillar multiplies the surface that must stay honest, and letting pillars import an SDK directly breaks the Task 4a import boundary that exists to keep every model call auditable. The refusal is pinned as a regression test (`test_narrative_grounding_rejects_the_assistant_envelope`) so the failure mode cannot silently return. Default-`None` behaviour is pinned by a contract test, so this change cannot drift into a behaviour change for the fisheries path. |
| 18 | 30 03:10 | `chat()` gains an optional `timeout_seconds` (default `None` = deployment-wide `GEMMA_TIMEOUT_SECONDS`). The port brief is cut to ~4 sentences. The transport route's ceiling was raised to **90 s, tested, and reverted to 60 s**. | Four live calls to the arrivals endpoint have now returned 503, a refusal, a 60 s timeout and a 90 s timeout. The refusal was fixed by entry 17. The latency hypothesis was that the narrative simply needed longer — it was tested and is **rejected by its own experiment**: with the shortened brief the call still timed out at 90 s (92.3 s wall). A longer ceiling bought nothing and cost every caller an extra 30 s before the identical deterministic fallback, so it is net-harmful and the value goes back down. The parameter and the route knob stay: the mechanism is right (the analyse path already carries per-route timeouts) and whoever diagnoses this next should change one number rather than re-plumb it. The prompt shrink stays regardless — a port officer wants four sentences, not an essay, so it is the better product independent of latency. |
